# Set in Vercel → Environment Variables (Production + Preview)
# Must be the full API base URL including /api
VITE_API_URL=https://recruit-pro-steel.vercel.app/api

# Local dev (Vite proxy also works with /api)
# VITE_API_URL=http://localhost:3001/api
